package com.api.project.ecommerce.config;

/**
 * This class contains application-wide constant values 
 * used for pagination and sorting configurations.
 */
public class AppConstants {
    
    /** Default page number for pagination. */
    public static final String PAGE_NUMBER = "0";
    
    /** Default page size for pagination. */
    public static final String PAGE_SIZE = "50";
    
    /** Default field to sort categories by. */
    public static final String SORT_CATEGORIES_BY = "categoryId";
    
    /** Default sorting direction (ascending or descending). */
    public static final String SORT_DIR = "asc";
}
